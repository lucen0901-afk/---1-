# Netlify 配置：本站是纯静态站，没有构建步骤。
# 有了这个文件，Netlify 在后台「连仓库」时会自动预填设置，
# 不会因为 Publish directory 填错（如 dist / public）而部署失败。
[build]
  publish = "."

# 301 重定向与响应头统一放在 _redirects / _headers 两个文件里。
# 这两个文件放在发布目录根下即可生效，Git 自动部署和手动 zip 直传都认，
# 比写在这里更容易迁移，也少一处配置漂移。
